name: Windows Cloud RDP
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360
    steps:
      - name: 1. Enable Remote Desktop & Firewall
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 0
      - name: 2. Create Admin Account
        run: |
          $Password = ConvertTo-SecureString "Qq123456...." -AsPlainText -Force
          New-LocalUser -Name "xxx123" -Password $Password -PasswordNeverExpires -ErrorAction SilentlyContinue
          Add-LocalGroupMember -Group "Administrators" -Member "xxx123" -ErrorAction SilentlyContinue
          net user runneradmin Qq123456....
      - name: 3. Install & Connect Tailscale VPN
        env:
          TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
        run: |
          # Download and install Tailscale silently
          Invoke-WebRequest "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile "C:\tailscale.msi"
          Start-Process msiexec -ArgumentList "/i C:\tailscale.msi /quiet /norestart" -Wait
          # Connect to Tailscale network
          Start-Sleep -Seconds 5
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey="$Env:TAILSCALE_AUTHKEY" --hostname="github-rdp-server"
          Start-Sleep -Seconds 8
          # Get Tailscale IP
          $ts_ip = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          Write-Host ""
          Write-Host "==========================================" -ForegroundColor Green
          Write-Host "  >>> RDP SERVER IS READY! <<<" -ForegroundColor Cyan
          Write-Host "  Tailscale IP : $ts_ip" -ForegroundColor Yellow
          Write-Host "  Username     : xxx123" -ForegroundColor Yellow
          Write-Host "  Password     : Qq123456...." -ForegroundColor Yellow
          Write-Host "  >> Open mstsc and connect to: $ts_ip" -ForegroundColor Magenta
          Write-Host "==========================================" -ForegroundColor Green
      - name: 4. Keep Server Alive (Web heartbeat 保活脚本)
        run: |
          while($true){
            $job = Start-Job -ScriptBlock {
              try{
                Invoke-WebRequest -Uri "https://baidu.com" -UseBasicParsing
                return $true
              }catch{
                return $false
              }
            }
            #最多等10秒，不管有没有结果
            Wait-Job $job -Timeout 10 | Out-Null
            if($job.State -eq 'Completed'){
                $ret = Receive-Job $job
                if($ret){
                    Write-Host "$(Get-Date) ✅ 访问成功"
                }else{
                    Write-Host "$(Get-Date) ❌ 访问失败"
                }
            }else{
                Write-Host "$(Get-Date) ⏱ 请求超时，强制终止"
                Stop-Job $job
            }
            Remove-Job $job -Force
            Start-Sleep -Seconds 110
          }
